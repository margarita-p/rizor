extends ./templates/template

block vars
    - var pageTitle = 'Lipsum'
    - var main = true

block content
    .content
        +intro({
            mix: 'content__section',
        })
        +features({
            mix: 'content__section',
        })
